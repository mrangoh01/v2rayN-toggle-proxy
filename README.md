# v2rayN-toggle-proxy
A lightweight Bash script that automatically enables or disables system-wide proxy settings in Ubuntu (guest in VirtualBox) based on the status of *v2rayN* on the Windows (Host).
